---
layout: page
title: Writing
eleventyExcludeFromCollections: true
---
<ul>
  {% for post in collections.post | reverse %}
    <li>
      <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
      <abbr title="{{post.date}}">utc: {{ post.date | readableDate }}</abbr>
  <abbr title="{{post.date}}">local: {{post.date | localDateTime }}</abbr>
  <abbr title="{{post.date}}">relative: {{post.date | relativeDate }}</abbr>
      <p>{{ post.data.description }}</p>
    </li>
  {% endfor %}
</ul>